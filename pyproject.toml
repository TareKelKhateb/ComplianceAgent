[project]
name = "complianceagent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0.3",
    "requests>=2.33.1",
    "dvc>=3.67.1",
    "dvc-s3>=3.3.0",
]

[dependency-groups]
dev = [
    "jupyterlab>=4.5.7",
]
